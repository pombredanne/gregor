@namespace("gregor.1")

protocol remind {

	/**
	 getReminders gets the reminders waiting to be sent out as a batch
	 */
	ReminderSet getReminders();

	/**
	 deleteReminders deletes all of the reminders by ReminderID
	 */
	void deleteReminders(array<ReminderID> msgs);
}
